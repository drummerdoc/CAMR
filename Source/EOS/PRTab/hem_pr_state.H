// PRTab forwarder: the PR state machinery has one definition, in ../PR.
// Forwarding rather than copying keeps the include guard deduplicating
// every route to that file.  Resolved via the -I on Source/.
#include "EOS/PR/hem_pr_state.H"
