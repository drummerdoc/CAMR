# HANDOFF — state of the Pelanti–Shyue work after F0–F5 (2026-08-17)

Successor to REVIEW_HANDOFF.md.  Everything below is measured and
recorded in WORKLOG.md (dated entries, predictions-first) and
STATUS_multiphase.md; this file is the map.

## 0. Discipline (unchanged, non-optional)
Predictions + falsifiers in WORKLOG BEFORE runs.  Counters split by
cause, never reasoned about.  Every dial default-off and
inertness-verified by battery diff.  Failures abort (D10).  Refutations
recorded.  §4 of REVIEW_HANDOFF.md remains the do-not-re-derive list.
D21 stands: every B-case HEM reference rewards the equilibrium limit;
cite BOTH bracket rows (HEM + FROZEN) for any B2/B9 claim.

## 1. The current defaults (one global configuration, no per-case dials)
> ADDENDUM 2026-08-17 (WORKLOG G-DEF, after this file was written): when
> written, this section described the ACCEPTANCE CONFIGURATION — the
> harnesses passed ps_relax_mode=5 and ps_flash_from_absent=1 on the
> command line while the code defaults were 0 and 0.  Both are now CODE
> defaults and the harnesses no longer re-state them, so a change to
> either is visible in the acceptance table.  Two caveats stay: (a) the
> three stiffness rates ps_theta_tau / ps_mt_tau / ps_flash_tau = 1e-7
> are still harness-set (they default to 0, at which X3 has no thermal
> rate, no MT and no flash and degenerates to mechanical equilibrium —
> MEASURED: the bare `inputs` run is bit-identical across the flip), so
> the acceptance configuration is FIVE dials, of which two are defaults;
> (b) the flip exposed a real defect — PS_sources.H's duplicate read of
> ps_relax_mode meant the split MT source ran on top of X3, the operator
> applied twice per step, until the dial was consolidated onto one
> accessor.
> ADDENDUM 2026-08-24 (THETA-DEFAULTS, Marc's call after the co2-eos
> audit): (1) the THETA FORK route (a) commit (0a3387d, post-dating this
> file) redefined theta <= 0 as INSTANTANEOUS thermal equilibrium — T1=T2
> joins the P1=P2 constraint — so caveat (a) below ("at 0 X3 has no
> thermal rate, no MT and no flash and degenerates to mechanical
> equilibrium") describes the PRE-fork code and is superseded; the
> bit-identity measurement it cites predates the fork.  (2) The three
> stiffness rates ps_theta_tau / ps_mt_tau / ps_flash_tau now DEFAULT to
> the acceptance value 1e-7, so CODE DEFAULTS ARE THE ACCEPTANCE
> CONFIGURATION and exact_suite passes NO relaxation dials for B cases —
> the G-DEF principle completed: any default change is visible in the
> acceptance table.  A bare `inputs` run now runs the acceptance physics.
> (A/C rows still pass ps_do_relax=0 — the historical sixth dial, AUDIT
> 2026-08-24 B3; dropping it is a separate measured flip.)  Battery
> re-verification of this flip is recorded in AUDIT_co2eos_2026-08-24.md.
* CAMR.ps_relax_mode = 5 — X3: the coupled relaxation source.  P1 = P2
  is an instantaneous DAE CONSTRAINT (alpha-adjusting projector at every
  path evaluation); thermal + MT are simultaneous finite rates on that
  manifold; sub-stepped BE; one eligibility question (coexist_action
  policy inside the kernel); F3 admissibility (c_k > 0, P_mix > 0,
  projection success).  Kernel: hem::ps_x3_relax_cell.  Acceptance:
  CAMR.ps_x3_test=1 (fixed point route/basin-independent, = HEM flash).
* CAMR.ps_flash_from_absent = 1 — the nucleator (flash from an ABSENT
  phase) is ON.  Selective by measurement: fires only where metastable
  windows open (B2/B9/B7); 8 B-cases bit-identical with it on.
* ps_mech_close, ps_mt_tau, ps_mt_form: mode-4-only A/B knobs now.
* MT rate everywhere: BE-SRT (Gamma = Sigma·rho_v·sqrt(M/2piRuT)·dg),
  geometry placeholder ps_mt_srt_d=0.1 / ps_mt_srt_delta=0.01.
* Y4 (per-case config) IS HISTORY: at the X3 default, action=3 adds
  nothing once FA is live (measured to the third decimal).

## 2. What was established (the short list, with instruments)
* Mode-4's thermal TARGET solve fails on order-one fronts (~57–69 K
  median-max residuals, [PS-DT]) — third instance of the failing-target
  family (MT eqfail 400/400; flash Illinois-NaN).  X3's rate-based BE is
  theory-exact in completeness (1/(1+dt/theta), measured on the nose).
* B7's 1811 K vapour was THAT defect, not B7 physics: relax channel
  +1449 K (mode-4 control) -> +3 K under X3; peak T2 345.9 K ([PS-T2]).
* The mode-4-vs-X3 score gap on B2/B9 at fast SRT rates was measured to
  be the HEM references rewarding the defect's hot-vapour artifact
  ([PS-DM] census: same mass, same cells, same timing; profile slices).
* Sigma kill test PASSED (coverage 1.000 on B2/B9, zero at contacts);
  Gamma_SRT at the F2-measured Sigma scale (~2e4 /m; srt_d ~ 1e-4)
  matches equilibrium-form quality to the third decimal (F3 sweep,
  monotone).  DESIGN_ps_sigma.md holds the scoped implementation.
* Both forms starve the evaporation wave (u ~ 150 m/s column vs ~0–10
  produced): interfacial-area limit.  That is THE open physics item.

## 3. Current acceptance baseline (mode 5 + FA, one config, N=64)
A/C unchanged (D22 headline invariant through everything).  B-cases:
B1 .0051/.1106/.0485 · B3 .0486/0/0 · B4 .0635/.1261/.0493 ·
B5 .0446/.1762/.0199 · B6 .0236/.0667/.0308 · B7 .1654/.7099/.3211 ·
B8 .0115/.1647/.0527 · B10 .0705/.1202/.0833 · B11 .0805/.0278/.0927 ·
B2 .0929/.7816/.1511 · B9 .1125/.7565/.1437 (both complete; FROZEN
bracket rows print at default — verify fresh after the flip commit).

## 4. What remains
1. Sigma, the seventh scalar — THE next plan.  DESIGN_ps_sigma.md gates
   G0 (inertness) G1 (wall gate: one global config reproduces every
   case's best — now largely pre-achieved by X3+FA; G1's real target is
   closing the evaporation-wave starvation via theta(Sigma) and
   Gamma_SRT(Sigma)) G2 (conservation/reflux) G3 (0-D) G4 (r_nuc decade
   sweep).  Prerequisite: r_nuc provenance citation + registered
   sensitivity sweep.  X3 is the substrate.
2. Backlog (unordered): r_K co-derived energy-partition derivation note;
   W2-1 energy-wave identity residual 4.3e-3; corridor face-state
   identity in 2-D; shared EOS interface header (PS+GammaLaw unguarded);
   X3 device twin (low priority, relax ~1% of step).
3. Marc-side: **git commits DONE 2026-08-17** — five commits on branch
   co2-eos (EOS backends; Source F0-F5 + instruments; harness; docs;
   G-DEF defaults + the one-read fix); working tree clean apart from
   scratch.  STILL OPEN on Marc's side: in the standalone repo ALL of
   suite/exact_*.csv are UNTRACKED, not just the two frozen ones — 16
   untracked reference CSVs, i.e. the entire acceptance reference set
   lives only on local disk (verified 2026-08-17 against co2-eos-cfd HEAD
   ffc87e5); purge _to_delete_stage2/, _to_delete_gitlocks/ and
   Exec/CO2_RiemannSuite/_to_delete_session/ plus _snap*.  Note: this
   session had to move two stale git lock files (.git/index.lock and an
   Aug-15 .git/HEAD.lock) aside into _to_delete_gitlocks/ because the
   device bridge cannot unlink files.

## 5. Environment notes for a successor session
Build: AMREX_HOME=$HOME/mnt/PeleLMeX/Submodules/PelePhysics/Submodules/amrex
make -j4 DIM=1 USE_MPI=FALSE Eos_Model=PR KEEP_BUILDINFO_CPP=TRUE in
Exec/CO2_RiemannSuite; incremental — resubmit on timeout; realclean when
GNUmakefile defines change.  Battery: CO2_STANDALONE=$HOME/mnt/co2-eos-cfd
python3 exact_suite.py [cases]; override-carrying runs via _stage2.py
(battery/diag modes).  No env-var knobs — CAMR.* ParmParse only.
Instruments: [PS-DT] [PS-DM] [PS-T2] [PS-PRES] [PS-COEXIT-TH]
[PS-MTCAUSE] [PS-FLASH-EV/REF], all diag-gated, all cause-split.
